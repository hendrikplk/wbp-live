'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
/* global Meteor, Mongo */

var client = {};
var collection = void 0;

if (typeof Meteor !== 'undefined' && Meteor.isClient) {
  collection = new Mongo.Collection('PublishJoin');

  client.get = function get(name) {
    var join = collection.findOne({
      _id: name
    });

    return join && join.value;
  };

  client.has = function has(name) {
    return collection.find({
      _id: name
    }, { limit: 1 }).count() > 0;
  };
}

var Client = exports.Client = client;
var Collection = exports.Collection = collection;